pywin32>=306
pillow>=10
requests>=2.31
PyQt6>=6.4.0
python-dotenv>=1.0.0
markdown>=3.5.0

# 批量嵌入和向量库相关依赖
# NumPy 2.x 兼容性：使用支持 NumPy 2.x 的 FAISS 版本
numpy>=1.25.0,<3.0
tqdm>=4.65.0
faiss-cpu>=1.8.0
qdrant-client>=1.7.0

# AI和RAG相关
google-generativeai>=0.3.0
google-genai>=0.1.0  # 新版Google GenAI SDK，支持from google import genai
bm25s>=0.2.13  # 新的BM25实现，更快更稳定
scikit-learn>=1.5.0
jieba>=0.42.1

# 打包工具
pyinstaller>=6.0.0

# WebView2 支持
pythonnet>=3.0.0  # WebView2 支持已启用

# BlurWindow半透明效果依赖
BlurWindow>=1.0.0

# Voice input support
vosk>=0.3.45
sounddevice>=0.5.0
